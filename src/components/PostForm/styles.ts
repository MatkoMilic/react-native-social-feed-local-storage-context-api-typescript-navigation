import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  postFormContainer: {
    flex: 1,
  },
  inputErrorMessage: {
    color: "red",
  },
  postButton: {
    backgroundColor: "red",
  },
});
